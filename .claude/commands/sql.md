Gere uma query SQL para: $ARGUMENTS

Antes de mostrar o código:
- Explique o que a query precisa fazer em português simples
- Se usar algum conceito SQL (JOIN, GROUP BY, subquery, etc.), explique o que é antes de usar

Formato da resposta:

1. **Entendendo o problema** — o que precisamos buscar/calcular?

2. **Conceitos utilizados** — explique cada cláusula SQL que vai aparecer

3. **A query** — código SQL limpo e comentado
   ```sql
   -- comentários explicando cada parte
   ```

4. **Como ler o resultado** — o que cada coluna do resultado significa

5. **Variações** — mostre como modificar a query para casos próximos

Use SQL padrão compatível com MySQL, PostgreSQL e SQLite. Se tabelas/esquema não forem fornecidos, pergunte antes de escrever.
